@using System.ComponentModel
@model List<VillaMVC.Models.DTO.VillaNumberDto>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

@if (TempData["NotValidId"] is not null)
{
    <div class="alert alert-dismissible alert-danger">
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        @TempData["NotValidId"]
    </div>
}
<partial name="_Notification"/>
<br/>
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Villa List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="VillaNumber" asp-action="Create" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i> Create New Villa</a>
        </div>
    </div>

    <table class="table">
        <thead>
        <tr>
            <th>Villa Number</th>
            <th>Special Details</th>
            <th>Villa ID Foreign Property</th>
            <th>Villa Name Foreign Propyrty</th>
            <th>Villa Details Foreign Propyrty</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.VillaNo</td>
                <td>@item.SpecialDetails</td>
                <td>@item.Villa.ID</td>
                <td>@item.Villa.Name</td>
                <td>@item.Villa.Details</td>
                <td>
                    <a asp-controller="VillaNumber" asp-action="Update" asp-route-id="@item.VillaNo" class="btn btn-success">
                        <i class="bi bi-pencil-square"></i>
                    </a>
                    <a asp-controller="VillaNumber" asp-action="Delete" asp-route-id="@item.VillaNo" class="btn btn-danger">
                        <i class="bi bi-trash-fill"></i>
                    </a>
                </td>
            </tr>
        }
        </tbody>
    </table>

</div>